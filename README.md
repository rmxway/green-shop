# 🌿 SPA Интернет-магазин Натуральной Продукции

Современный интернет-магазин натуральной продукции на Next.js 15 с React 19.

## ✨ Ключевые возможности

- **Каталог товаров** с поиском, фильтрами и пагинацией
- **Многоязычная поддержка валют** (USD/RUB) с автоматической конвертацией
- **Корзина покупок** с сохранением в localStorage
- **Избранные товары** с отдельной страницей
- **Сравнение товаров** — таблица сравнения характеристик выбранных товаров
- **Аутентификация и авторизация** — регистрация, вход, личный кабинет
- **История заказов** — просмотр всех оформленных заказов с детализацией
- **Профиль пользователя** — управление данными и автозаполнение формы заказа
- **Оформление заказа** — доступно как для зарегистрированных пользователей, так и для гостей
- **Тёмная и светлая темы** с переключателем и сохранением выбора пользователя
- **Адаптивный дизайн** для всех устройств
- **Современный стек**: Next.js, React, TypeScript, Redux, Styled Components

## 🛠️ Технологии

**Frontend**: Next.js 15, React 19, TypeScript, Styled Components, Framer Motion

**State**: Redux Toolkit, Redux Persist, RTK Query

**Аутентификация**: NextAuth.js (Credentials Provider)

**База данных**: Firebase Firestore

**Формы**: React Hook Form, Yup

**UI**: Swiper, React Loading Skeleton

**Валюты**: Интеграция с API Центрального Банка РФ для актуальных курсов

**Инструменты**: ESLint, Prettier, Jest, React Testing Library, Fantasticon, Storybook

## 🚀 Быстрый старт

### Установка

```bash
git clone https://github.com/rmxway/green-shop
cd green-shop
yarn
```

### Настройка переменных окружения

Для работы аутентификации, Firebase и заказов нужны переменные окружения. Файл с секретами (`.env.local`) в репозиторий не входит — так и должно быть.

1. Скопируйте пример конфигурации в свой локальный файл:

```bash
cp .env.local.example .env.local
```

2. Откройте `.env.local` и заполните значениями из вашего проекта Firebase и сгенерированным секретом NextAuth (см. [AUTH_SETUP.md](./AUTH_SETUP.md)).

3. Для генерации `NEXTAUTH_SECRET`:

```bash
openssl rand -base64 32
```

Переменные из `.env.local.example`:

- **Firebase (клиент)** — из Firebase Console → Project settings → Your apps → Web app
- **Firebase Admin** — из Firebase Console → Service accounts → Generate new private key (JSON), поля `project_id`, `client_email`, `private_key`
- **NEXTAUTH_URL** — для локальной разработки оставьте `http://localhost:3000`
- **NEXTAUTH_SECRET** — обязательно сгенерируйте свой и не публикуйте

**Подробная инструкция по настройке Firebase и аутентификации:** [AUTH_SETUP.md](./AUTH_SETUP.md)

### Запуск

```bash
yarn dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 📚 Storybook

В проекте настроен [Storybook](https://storybook.js.org/) для изолированной разработки и документации UI-компонентов.

**Онлайн-документация** :  
[Storybook на GitHub Pages](https://rmxway.github.io/green-shop/)

Локальный запуск:

```bash
yarn storybook
```

Откройте [http://localhost:6006](http://localhost:6006) — там доступны сторисы для UI-компонентов (кнопки, модалки, стикеры, карточки товаров и др.) с разными вариантами и интерактивными контролами.

Сборка статической версии для деплоя: `yarn build-storybook`. Деплой на GitHub Pages выполняется автоматически через GitHub Actions при пуше в `main`/`master`.

### Основные команды

```bash
yarn dev      # Запуск разработки
yarn build    # Сборка для продакшена
yarn start    # Запуск продакшен версии
yarn test     # Запуск тестов
yarn lint     # Проверка кода
yarn icofont  # Генерация иконок
yarn storybook        # Запуск Storybook (http://localhost:6006)
yarn build-storybook  # Сборка Storybook для деплоя
```

## 🎯 Функции

- 🔍 **Поиск** и фильтрация товаров
- 💱 **Многоязычная валюта** — переключение между USD и RUB с актуальными курсами
- 🛒 **Корзина** с формой заказа
- ❤️ **Избранное** с сохранением
- ⚖️ **Сравнение** — добавление товаров в сравнение и таблица характеристик
- 🔐 **Аутентификация** — регистрация и вход в систему
- 👤 **Личный кабинет** — управление профилем и просмотр истории заказов
- 📦 **История заказов** — детальная информация о всех оформленных заказах
- 🌗 **Переключение темы** — светлая/тёмная темы интерфейса
- 📱 **Адаптивность** для мобильных устройств

## 🎨 Тёмная тема

- Тема приложения управляется через контекст `ThemeModeProvider` и `styled-components` темы (`defaultTheme` / `darkTheme`).
- Выбор пользователя (`light` или `dark`) сохраняется в `localStorage` под ключом `green-shop-theme` и восстанавливается при следующем заходе.
- Переключатель темы доступен в интерфейсе и изменяет режим без перезагрузки страницы.

## 🔧 API

- **RTK Query** для работы с [dummyjson API](https://dummyjson.com/)
- **Интеграция с API ЦБ РФ** для получения актуальных курсов валют
- **Firebase Firestore** для хранения пользователей и заказов
- **NextAuth.js** для аутентификации и управления сессиями
- Данные о товарах загружаются через RTK Query endpoints в `store/api.ts`
- Кеширование курсов валют на 24 часа для оптимизации производительности

### API Endpoints

- `POST /api/register` - Регистрация нового пользователя
- `POST /api/auth/[...nextauth]` - NextAuth endpoints (логин, логаут, сессия)
- `GET /api/profile` - Получение профиля текущего пользователя
- `PUT /api/profile` - Обновление профиля
- `POST /api/order` - Создание заказа (авторизованный или гость)
- `GET /api/orders` - Получение списка заказов текущего пользователя

### Маршруты приложения

**Публичные страницы:**

- `/` - Главная
- `/products` - Каталог товаров
- `/product/[id]` - Страница товара
- `/favorites` - Избранные товары
- `/compare` - Сравнение товаров
- `/login` - Вход
- `/register` - Регистрация
- `/cart` - Корзина (оформление заказа доступно всем)

**Защищённые страницы:**

- `/account` - Личный кабинет (требуется авторизация)
- `/account/orders` - История заказов (требуется авторизация)

## 📦 Развертывание

Проект развернут на Vercel: [🌐 Демо](https://green-shop-brand.vercel.app/)

### Требования для развертывания

1. Настроить проект Firebase и получить конфигурацию
2. Добавить переменные окружения в настройках Vercel (список см. в `.env.local.example`)
3. Настроить правила безопасности Firestore для продакшена

Подробнее о настройке аутентификации см. [AUTH_SETUP.md](./AUTH_SETUP.md)
